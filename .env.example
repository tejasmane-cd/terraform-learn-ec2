# Copy this file to .env for local use only.
# Do not commit .env.

AWS_REGION=us-east-1
AWS_PROFILE=default

# Prefer setting this to your own IP/CIDR when planning locally.
# Example: TF_VAR_allowed_ssh_cidr=203.0.113.10/32
TF_VAR_allowed_ssh_cidr=0.0.0.0/0
